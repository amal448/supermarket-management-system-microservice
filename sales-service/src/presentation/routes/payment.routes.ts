// import { Router } from "express";

// import { stripeWebhook } from "../controllers/payment.controller";


// const router = Router();

// router.post("/stripe-webhook", bodyParser.raw({ type: "application/json" }), );

// export default router;
